# 代码仓库数据存储架构说明

## 📊 保存的数据类型

### 1. 文件数据（code_files）
- 文件路径、文件名、语言类型
- 代码行数、文件大小
- 复杂度评分、符号数量、导入数量
- 内容哈希（用于变更检测）
- 文件与仓库的关系

### 2. 符号数据（code_symbols）
- 符号名称、类型（函数/类/方法/变量）
- 完整限定名、函数签名
- 返回类型、参数列表（JSON）
- 位置信息（起始行、结束行）
- 文档字符串、复杂度评分
- 符号与文件的关系

---

## 🗄️ 为什么保存到 MySQL？

### MySQL 的主要用途：

#### ✅ **1. 精确统计查询**
```sql
-- 统计文件数、代码行数、语言分布
SELECT language, COUNT(*) as count, SUM(lines_of_code) as lines
FROM code_files
WHERE repository_id = 54
GROUP BY language;

-- 统计函数数、类数
SELECT symbol_type, COUNT(*) as count
FROM code_symbols
WHERE repository_id = 54 AND symbol_type IN ('function', 'class')
GROUP BY symbol_type;
```
- **精确计数**：MySQL 的 `COUNT()`、`SUM()`、`GROUP BY` 非常适合统计
- **性能优秀**：索引优化，查询速度快
- **实时性**：前端统计页面需要实时展示准确数据

#### ✅ **2. 关系型数据管理**
- **外键约束**：`code_symbols.file_id` → `code_files.id`
- **级联删除**：删除文件时自动删除相关符号
- **数据一致性**：事务保证

#### ✅ **3. 结构化查询**
- **复杂过滤**：`WHERE repository_id = ? AND language = ? AND is_deleted = false`
- **分页查询**：`LIMIT`、`OFFSET`
- **排序**：`ORDER BY`

#### ✅ **4. 前端页面需求**
- **统计信息页面**：需要显示文件总数、代码行数、函数数、类数、语言分布
- **文件列表**：需要按语言、仓库筛选
- **符号列表**：需要按类型、文件筛选

---

## 🔍 OpenSearch 的作用

### OpenSearch 的主要用途：

#### ✅ **1. 语义搜索（向量搜索）**
```python
# 将代码内容转换为向量，支持语义相似度搜索
query_vector = vectorize("如何实现用户登录功能？")
results = opensearch.knn_search(
    index="code_symbols",
    vector=query_vector,
    k=10
)
```
- **语义理解**：理解代码的语义含义
- **相似度搜索**：找到语义相似的代码片段

#### ✅ **2. 全文搜索**
- **关键词搜索**：搜索函数名、变量名
- **模糊匹配**：支持部分匹配
- **分词搜索**：中文、英文分词

#### ✅ **3. 存储完整内容**
- **代码片段**：存储符号对应的代码内容（最多5000字符）
- **向量数据**：存储高维向量（用于 KNN 搜索）
- **元数据**：存储完整的符号/文件信息

#### ❌ **OpenSearch 不适合的用途**
- **精确统计**：`COUNT()` 性能较差，不适合频繁统计查询
- **复杂 JOIN**：不支持 SQL JOIN，需要多次查询
- **事务**：不支持事务，数据一致性不如 MySQL
- **关系型查询**：外键、级联等需要应用层实现

---

## 🤔 是否可以只保存到 OpenSearch？

### 技术可行性：✅ 可行
- OpenSearch 可以存储所有元数据
- 可以存储向量和全文内容

### 但不推荐的原因：❌

#### 1. **统计查询性能差**
```python
# OpenSearch 统计查询（慢）
result = opensearch.search(
    index="code_files",
    body={
        "size": 0,
        "aggs": {
            "languages": {
                "terms": {"field": "language"}
            }
        }
    }
)

# MySQL 统计查询（快）
result = db.query(
    func.count(CodeFile.id),
    CodeFile.language
).group_by(CodeFile.language).all()
```

#### 2. **无法使用关系型特性**
- 无法使用外键约束
- 无法使用级联删除
- 无法使用 JOIN 查询

#### 3. **事务支持差**
- OpenSearch 不支持 ACID 事务
- 数据一致性需要应用层保证

#### 4. **查询复杂度高**
- 简单的统计查询需要写复杂的 DSL
- SQL 更直观、易维护

---

## 🏗️ 推荐的混合架构

### 当前架构（推荐）✅

```
┌─────────────────────────────────────────────────────┐
│                  代码解析结果                         │
└─────────────────────────────────────────────────────┘
           │
           ├─────────────────┬─────────────────┬──────────────┐
           │                 │                 │              │
           ▼                 ▼                 ▼              ▼
    ┌──────────┐     ┌─────────────┐  ┌──────────┐  ┌──────────┐
    │  MySQL   │     │ NebulaGraph │  │OpenSearch│  │  Redis   │
    │          │     │             │  │          │  │          │
    │ ✅ 统计  │     │ ✅ 图谱关系 │  │ ✅ 搜索  │  │ ✅ 缓存  │
    │ ✅ 关系  │     │ ✅ 依赖分析 │  │ ✅ 向量  │  │          │
    │ ✅ 查询  │     │             │  │          │  │          │
    └──────────┘     └─────────────┘  └──────────┘  └──────────┘
```

### 各存储的职责分工：

| 存储 | 用途 | 数据特点 |
|------|------|----------|
| **MySQL** | 结构化数据、统计查询 | 文件/符号元数据、关系 |
| **NebulaGraph** | 图谱关系、依赖分析 | 节点、边、图谱查询 |
| **OpenSearch** | 语义搜索、全文搜索 | 向量、全文索引 |
| **Redis** | 缓存、任务队列 | 临时数据、锁 |

### 数据同步流程：

```
1. 代码解析
   ↓
2. 保存到 MySQL（元数据、关系）
   ↓
3. 保存到 NebulaGraph（图谱节点和边）
   ↓
4. 向量化并保存到 OpenSearch（向量、全文）
   ↓
5. 查询时根据需求选择：
   - 统计信息 → MySQL
   - 文件列表 → NebulaGraph 或 MySQL
   - 语义搜索 → OpenSearch
   - 依赖关系 → NebulaGraph
```

---

## 💡 总结

### 为什么要保存到 MySQL？
1. **统计查询**：前端统计页面需要精确的计数和聚合
2. **关系管理**：文件-符号关系需要外键和级联
3. **查询性能**：结构化查询 MySQL 性能更好
4. **数据一致性**：事务保证数据一致性

### OpenSearch 的作用是什么？
1. **语义搜索**：理解代码语义，找到相似的代码
2. **全文搜索**：关键词、模糊匹配
3. **向量存储**：存储代码的向量表示

### 是否可以只保存到 OpenSearch？
- **技术上可行**，但**不推荐**
- MySQL 更适合统计、关系查询
- OpenSearch 更适合搜索、语义匹配
- **混合使用**才能发挥各自优势

### 最佳实践
- **MySQL**：结构化数据、统计、关系
- **OpenSearch**：搜索、向量、全文
- **NebulaGraph**：图谱、依赖关系
- **各司其职，优势互补** 🎯
